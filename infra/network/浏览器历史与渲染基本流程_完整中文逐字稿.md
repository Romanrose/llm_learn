# 30 分钟弄懂浏览器历史与渲染基本流程：完整中文逐字稿

- 视频：[Bilibili：30分钟弄懂浏览器历史与渲染基本流程 / 浏览器工作原理入门教程](https://www.bilibili.com/video/BV1tc41157Va/)
- 字幕来源：Bilibili `ai-zh` 中文字幕（AI 生成字幕）
- 整理方式：保留段落起始时间戳；将连续语义合并为自然段，仅修正明显的断句、重复和浏览器术语错词。
- 生成日期：2026-08-06

---

## 正文

[00:00:00] Hello，大家好我相信绝大多数小伙伴也都是通过浏览器第一次了解互联网的，那么这个用了一辈子的东西大家真的了解过它是怎么运行的吗，那这个视频我讲的比较深入但也会照顾一下没有太多基础的小伙伴们我会给大家提前阐述一下相关的概念这期视频我会全程拿Chrome浏览器来举例，至于为什么拿Chrome来举例我们先从浏览器的历史来讲解在一个非常遥远的年代也就是1990年蒂姆·伯纳斯-李（Tim Berners-Lee）开发出第一个浏览器World Wide Web啊听了第一句话大家可能有点懵逼，首先解释一下万维网和互联网有啥区别互联网其实就是大家经常说的internet是一个基于TCP/IP协议族的计算机网络协议其实就是保证双方计算机都可以识别的一种约定的数据格式而万维网则是伯纳斯-李这个老哥发明的一个基于互联网访问的有超链接和统一资源标识符的文件和其他资源的全球集合啊也就是大家嘴里常说的World Wide WebWWW或者是web比如说web浏览器说的就是web网浏览器。

[00:01:03] 但是这个老哥发明的第一个浏览器World Wide Web并不是指web网只不过他俩撞名了后来因为撞名了这个问题把浏览器的名字也给改了，那接下来跟大家详细讲一下这个万维网它是由三个部分组成的第一个部分就是全球网络资源唯一认证的系统就是统一资源标识符第二个就是超文本标记语言也就是HTML第三个就是超文本传输协议也就是传输HTML的一个协议就是HTTP，那先跟大家逐一解释一下超文本是什么东西其实超文本就是还有指向其他资源链接的文本之前咱们写文字只能是写作文可是如果咱们可以把内容指向视频或者链接也就是在文本中添加hyperlink也就是超链接，那么这个文本本身就不是单纯的文本了，所以可以叫超文本，那么标记语言其实就是通过一些标签来标记相应的元素比如说一个超链接的内容通常是通过A标签来把它包住。

[00:01:58] 然后并且标识出的文本段通常是用P标签来标识的，那么这些标签内的内容就会被浏览器识别为超链接或者文本段相应的如果我们想要表示一个图片或者视频就需要通过一个位置来指出他们在哪里，所以就有了URI也就是Uniform Resource Identifier就是统一资源标识符它是由一个字符串来表示抽象或者物理的资源，那么除了 URI我觉得大家一定听说过URL其实就是Uniform Resource Locator也就是统一资源定位符这里简单说一下它俩的区别URI 是一种语义上的抽象概念而URL是URI的一种具体实现的形式大家说的网址就是互联网中网络资源的URL，那比如说我们可以通过HTTP://www.baidu.com来寻找百度这个网站上的资源。

[00:02:48] 接下来咱们再回头讲浏览器在World Wide Web浏览器开发完成之后1992年马克·安德森（Marc Andreessen）在NCSA也就是国家超级计算应用中心工作他带领团队研究出了Mosaic浏览器，那这个Mosaic其实就是马赛克的意思马赛克浏览器已经发布的这个万维网其实才算真正的活下来了因为马赛克参加了一个图形接口之前图片和文字混合的这种形式其实并不常见后来等这个安德森这个老哥毕业之后对他创建Mosaic的时候其实还没有毕业，所以他毕了业之后就去创建了一个Mosaic Communications后来因为NCSA拿着这个Mosaic浏览器的版权，所以就告那个Mosaic Communications侵权嘛后来安德森就把这个Mosaic公司改成了叫Netscape也就是非常著名的网景公司。

[00:03:38] 那后来他又和另外两个大哥写出了网景浏览器并逐渐成为了最流行的一个浏览器，那95年的时候微软以Mosaic浏览器的基础上研开发出了IE1.0浏览器，然后于是开始跟网景进行竞争后来发现也pk不过呀开始就多么歪心思就是跟windows捆绑进行销售，所以windows默认的浏览器都是IE浏览器当然这个操作也是所有网络开发者的一个噩梦因为几乎中国所有的学校政府都是用默认的IE浏览器很多项目验收的标准也都是在IE上能跑就行但好在微软前面真的顶不住了不再支持IE了啊，不过IE干人事的时候就是IE3.0是最先支持CSS 标准的浏览器，那之所以浏览器有各种各样漂亮的样式都是用CSS那结合成本身IE团队的一个能力，然后再加上捆绑销售这个策略IE的市场占有率一度高达90%以上。

[00:04:32] 所以这段历史也被称作第一次浏览器大战啊结果以IE彻底把网景干翻而结束，但是网景被干翻之后肯定是不服气的，所以在98年成立了Mozilla基金会并且把他97年的一个开发的Gecko 渲染引擎开源啊开源其实为开发源代码别人就可以根据开源协议在允许的范围内进行二次开发渲染引擎就是浏览器解析的页面的工具了本视频后部分会详细讲一下渲染引擎的工作原理，那么在当年苹果也没有闲着苹果参与了KDE 开发社区开发了一个叫KHTML的渲染引擎项目跟着开发了3年因为分歧的原因就自己创建了自己的WebKit引擎接着时间一晃来到了02年Mozilla创建了一个基于Gecko引擎的浏览器项目有意思的是有点重铸网址回望的感觉浏览器的名字最终被定为Firefox。

[00:05:23] 但是没有正式发布03年苹果发布了Safari浏览器没啥大风大浪主要是因为Mac专属用户太少了04年Firefox正式发布这个Firefox 1发布就好像网景浏览器绘图转生一样直接跟IE又干起来了在同期LINUX市场也开始预装Firefox成果还是可以的在08年Chrome还没有发布之前，那Firefox的市场占有率一度高达30%，但是05年的时候苹果开发了WebKit可以说这个举动直接埋下了IE被干翻的一个种子因为08年谷歌发布以 WebKit 为渲染引擎的Chromium同时发布了Chrome并且Safari不同的是他们的脚本引擎改成了更快的 V8 引擎这里的WebKit为渲染引擎是渲染页面用的V8 JavaScript 引擎是用来解析并执行脚本的因为速度很快。

[00:06:11] 所以这也成为了日后Chrome崛起的一个决定性因素V8日后也被用在了Node.js和Electron等技术上极大的丰富了JavaScript的使用场景这里的Chromium可以理解为开源的浏览器软件而Chrome可以理解为Chromium加谷歌服务的一个商业浏览器软件，那10年苹果宣布发布WebKit 2，那13年谷歌宣布放弃了WebKit 2改为自家Blink渲染引擎主要因为Chrome的多进程模型和其他的WebKit浏览器并不相同这几年因为这些架构差异搞得有点累觉不爱了，所以就直接撂挑子不干自己开发了，那因为Chrome基于多进程模型还有V8的高效快速很快占领了市场国产几乎所有浏览器都是基于Chromium开发的。

[00:06:53] 那么问题来了为什么没有人用Firefox 的 Gecko 引擎进行开发呢主要是因为Gecko 是基于 MPL 2.0协议进行开发的，所以基于Gecko的开发的浏览器都要公开源代码Chromium是基于BSD开源协议的不用公开源代码，不过其实实际上开发浏览器内核是非常复杂的不亚于一个操作系统国产浏览器厂商没有那么大的精力去自行研发业务无可厚非使用Chrome其实也能更好的帮助开发者去兼容一些新的特性我觉得也挺好，那Chrome的崛起也预示着IE的衰败后面IE实在顶不住了直接放弃了开发充足了各种代码之后开发出了EdgeHTML引擎并在15年发布了Edge 浏览器，但是大势已去，那现在大家主要拿它来下Chrome或者看PDF后面AJ的方向其实也是不怎么地。

[00:07:40] 所以后来微软放弃了EdgeHTML引擎的开发直接转而选用了 Chromium 的内核，所以到目前为止不管大家用Edge 也好或者国产浏览器或者Chrome也好大家其实都是一家子只不过界面和用户服务不同本质上都是差不多好的，那么历史讲完了，接下来再讲渲染原理之前我们先来了解一下一些基础概念，那首先我们需要了解一下进程的概念进程就是process它是一个程序的运行实例当我们启动一个程序，那么操作系统会创建一块内存给代码和运行时的数据使用并且创建一个线程来处理任务，那么这样一个环境就叫做进程，那么线程是什么玩意呢它是一个进程的执行任务或者也可以叫做控制单元负责当前进程中程序的执行一个进程至少有一个线程一个进程也可以运行多个线程多个线程之间可以进行数据的共享他们有如下几个特点第一个就是进程中一旦一个线程出现了问题。

[00:08:37] 那么整个进程就会崩溃掉第二点是在同一进程中不同的线程之间是可以进行数据共享的第三点进程关闭后内存会正确地进行一个回收第四点不同进程之间的内容相互隔离如果想要访问的话就需要依靠进程间通信机制也就是IPC，那么在Chrome出现之前绝大多数浏览器都是单进程的单进程浏览器的所有功能模块都是在同一进程中运行比如说网络插件渲染引擎和页面线程都是在同一个进程内，那么这样的操作就会带来一些蛋疼的问题，那首先就是不稳定如果我们使用的一个插件出现了错误，那么整个进程就会跟着崩必须要对浏览器进行重启第二点就是很容易卡因为所有页面的功能模块都在一个进程中如果其中一个页面卡住了，那么所有页面都跟着一起卡第三点就是容易发生内存泄露问题因为所有页面都在一个进程中单个页面有时候数据因为代码编写问题而出现内存泄露在关闭页面时泄露的内存并不会进行回收导致浏览器应用卡在单进程浏览器中插件和渲染线程也有很高的权限因为插件和渲染线程中包括各种脚本代码而这些脚本代码通常是由第三方来编写的如果有恶意插件或者网站的话他们可以通过这些权限来读写本地数据并对数据进行一个窃取。

[00:09:55] 所以为了解决这些问题Chrome采用了多进程架构多进程架构其实就是每个页面有单独的渲染进程和插件进程并且把每个页面的渲染进程和插件进程都装进沙箱内让他们不能获取到系统权限有关的权限问题统一交给浏览器主进程来操作不同进程之间可以通过IPC来传输数据Chrome如今有几大进程分别为浏览器进程渲染进程插件进程网络进程和GPU进程它们的作用分别为浏览器进程主要负责页面的展示用户的交互管理子进程还有提供存储功能网络进程主要负责下载网络资源GPU进程主要负责绘制网页和 UI 界面渲染进程包括JS的V8引擎和排版引擎Blink它的主要功能是将HTML、CSS 和 JavaScript转化成网页而最后的插件进程负责加载和运行页面上的插件在讲页面渲染之前还得补充一些额外关于网络协议的知识通过各种网络协议互联网中的设备才能互相沟通如果大家已经对计算机网络比较熟悉了可以根据下方视频的分解快速调到渲染步骤的讲解。

[00:11:04] 首先咱们来讲讲IP协议也就是internetprotocol就是互联网协议它是一个网络层协议两台设备相互传输数据需要遵守互联网协议我们口中常说的互联网其实就是基于IP协议的一个体系架构想要进行数据的传输每个主机都要有特定的IP地址我们知道了IP协议计算机可以通过IP协议在网络中互相传输数据，但是这个IP通常很不好记，所以我们有了域名来指代例如我们只需要通过输入百度点com就可以自动找到百度服务器的IP地址这个过程其实用到了DNSDNS其实就是Domain Name System中文名叫域名系统当我们发送请求时我们可以通过各种应用服务器来寻找到我们想要访问的IP地址，但是找到具体服务器的IP 之后我们应该怎么制定访问的具体程序这里我们用到了UDP和TCP协议。

[00:11:56] 首先我们讲一下UDP协议也就是User Datagram Protocol用户数据报协议有了用户数据报协议我们就可以通过端口号来访问指定的程序，但是UDP协议发送数据时不能保证接收端一定接收到数据丢了就是丢了为了解决这个问题工程师们研究出了TCP协议TCP协议也就是Transmission Control Protocol啊中文名字叫传输控制协议它跟UDP协议一样是一个传输层协议，但是它解决了UDP传输不可靠的缺点对于数据包的缺失它提供了一些重传机制另外TCP协议是面向连接的在传输数据之前他会和目标设备进行连接在传输完成后会和目标进行断开连接创建和断开的过程其实就是著名的三次握手和四次挥手具体就不展开讲了因为这点协议每个都能单独写一本书要细讲的话实在是养不动。

[00:12:48] 接下来数据就可以稳定的传输到指定的机器的浏览器中了，但是你不能乱传对不对传到浏览器的数据也是要有一定的格式的这样才能方便让浏览器对数据进行一个解读，所以这里规定了HTTP 协议也就是超文本传输协议因为这个在前面讲了也不细讲了，接下来协议讲的差不多了我们来讲一讲浏览器发送HTTP请求的流程，首先我们可以通过URL来请求服务器让它返回具体的文件在真的请求开始之前浏览器会找有没有上次请求缓存下来的文件如果这个缓存文件存在而且它还没有过期，那么我们就可以暂停我们的请求这么做可以让我们的网页更快速的被加载也可以缓解服务器的压力如果没有缓存的话，接下来我们通过HTTP协议做应用层协议通过TCP/IP协议来将它们发送到网络中在发送之前会让DNS去通过域名来查找相应的服务器的IP地址查询到以后就会对他们进行缓存这样下次我们就可以直接使用对应的IP如果我们输入的域名没有加端口号的话浏览器会默认给URL加上80 端口因为80 为 HTTP 协议的默认端口。

[00:13:56] 接下来我们开始准备为TCP建立连接进行三次握手握手完成之后我们就可以正式的发送我们HTTP的请求了，接下来就是服务端接收到请求并返回数据，那当然这里只是一个简化的流程仅仅是帮助大家对协议有一个具体的了解，接下来我们开始进入正题讲讲Chrome的工作原理这个问题其实也可以被理解成从输入URL到页面展示中间发生了什么，首先就是输入URL或其他内容时浏览器会根据你输入的内容进行一个判断因为现在浏览器地址栏也集成了搜索功能如果你输入的内容不符合URL的规则它会根据浏览器的默认引擎来合成带有搜索关键字的 URL如果符合URL的规则则浏览器会自动给你输入URL加上协议，那接下来就到了请求的阶段浏览器在发送请求的时候会让浏览器的主进程通过进程间通信把URL发送给网络进程紧接着浏览器的标签页的图标开始转圈。

[00:14:55] 但是当前页面还是老的页面当网络进程接收到服务器返回的HTTP响应头时发现响应的 MIME 类型为 HTML时就会让浏览器开启一个渲染进程并给他发送一个提交文档的命令同时网络进程传递文档信息也就是HTTP的响应体给渲染进程传递完毕后渲染进程会给浏览器进程发送一个接收完毕的命令，那么当前页面就会被白屏所替代，那接下来渲染进程联合GPU进程开始渲染渲染完毕后的页面返回给浏览器主进程进行展示上面是简化版的正常情况其实步骤还是很复杂的，所以接下来会对这个流程进行一个详细的解读，那么首先网络进程一般会查询本地是否有缓存资源如果有则直接返回给浏览器进程并中断请求如果没有进入网络流程第一步是进行DNS解析获取域名的服务器的IP地址如果协议是HTTPS需要建立TLS连接连接建立之后浏览器会构建请求头和请求体向服务器发送请求命令服务端接收到请求后会生成响应数据并返回给网络进程当网络进程接受的响应头后就开始解析响应头的内容了当响应头的状态码可能是301或者302这时候需要根据响应头的location的信息进行一个重定向并再次对新的URL进行一个请求的操作比如说这里我们请求了百度点com。

[00:16:20] 但是可以看到我们的浏览器最终请求的是3W点百度点com这是为什么呢就是因为当我们请求百度点com的时候百度点com的服务器给我们返回了一个重定向到3W点百度点com的请求如果没有重定向的操作当状态码为 200时，那么可以确定我们这次的请求是一个成功的根据响应头的Content-Type 内容，浏览器会有不同的操作如果响应类型为application/octet-stream，那么浏览器就会自动开启下载进程开启下载流程一般网页的话会是text/html在这种情况下浏览器会视情况开启条渲染进程，但是为什么说要视情况开启渲染进程呢因为在同一站点打开新页面时渲染进程是公用的，那同一站点是啥意思呢就是域名与协议相同的站点就是同一站点比如说HTTP://www.baidu.com跟域名为百度点com协议为HTTP。

[00:17:17] 那我们从3W点百度点com打开文库点百度点com就不会创建新的渲染进程除此之外咱们每次都要新建一个渲染进程，那新建好渲染进程之后浏览器进程发起提交文档消息渲染进程接收到消息之后会和网络进程建立数据连接通道并读取HTML 数据等接收完毕后渲染进程会返回确认提交的信息给浏览器进程浏览器进程收到消息后会出现白屏等待渲染进程对页面进行一个渲染，那接下来就是渲染流程了按照渲染的顺序我们可以把渲染流程分为如下几个阶段分别为构建DOM树计算样式布局分层绘制分块光栅化合成，那在讲渲染之前还是给大家铺垫一下一些内容就是网页的三件套HTMLCSS和JavaScriptHTML开头跟大家讲过了就是超文本标记语言它是网页的骨架由各式各样的标签组成标签可以代表网络的组成元素的类型CSS为层叠样式表它可以赋予网络标签各种好看的样式并且可以更改他们在网页中的排版让网络更好看JavaScript一般称呼为JS它可以给网络赋予各种各样的逻辑让网络变得具有交互性。

[00:18:37] 那接下来咱们可以正式的开始讲我们的渲染流程了，首先就是构建DOM树因为浏览器不认识HTML，所以要将它通过HTML 解析器转化成DOM 树供后续的步骤使用DOM 树可以理解为长像树的一种数据结构树也可以理解为我们平时用的思维导图啊或者大纲一样的结构HTML 解析并不是等整个文档加载完之后再去解析的而是网络进程加载了多少数据HTML 解析器便会解析多种数据网络进程会把实时获取到的数据源源不断地传递给渲染进程，然后交给HTML 解析器，那么通过分词器可以把开始标签内容和结束标签识别并分开，那我们可以把这些分词器分开的东西叫做token，那么接下来就是将token转化为DOM节点并且添加到DOM树中，那大家可能会迷惑这些 token是怎么储存的如果一个父节点包含了多个节点因为网络进程是一点一点的读取的。

[00:19:35] 那这个标签的解析器怎么判断他们的父子关系呢这里解析器把token放到了一个栈里大家在这里可以把这理解成一个杯子，然后这个token其实是一块一块的小积木这个积木只能从栈顶上拿出来不能从杯底拿出来解析器遇到开始标签这个标签会直接放到这里并且在DOM 结构中创建一个标签对应的节点，接下来解析器一般还是会收到很多开始标签这时候还是把它们依次添加到这里并且在DOM中生成相应的节点直到解析器遇到标签内的内容时会把内容添加到DOM结构中，最后添加的标签里并且继续接收其他的内容当接收到闭合标签时这时候这个标签不入栈并且会让这个标签的开始标签出栈这个节点也算是完成了如果下一个标签是开始标签的话，那把这个标签入栈并且在DOM 上的上一个节点的旁边创建一个兄弟节点。

[00:20:30] 那节点就是这个标签，接下来的过程也都类似但并不是所有的标签都是元素标签还有遇到script标签的时候及到script标签时HTML 解析器因为觉得script标签内的脚本可能会修改DOM，所以会停止进行解析脚本执行完成之后IHTML 解析器会恢复解析的过程继续解析后续的内容直到生成最终的DOM 树如果script标签是应用格式的话，那么会让网络进程先去下载这段代码并且在下载过程中也会让HTML 解析器停止解析而且一般下载的时间因为网络问题可能会非常长，所以这里Chrome会做一个优化叫做预解析操作渲染引擎在一开始受到字节流的时候会先开启一个预解析线程用来查找HTML文件中有关JS和CSS文件解析到相关文件以后预解析线程会提前进行下载我们可以通过在开发者工具选择Console工具通过输入document回车来查看document的变量其实这就是我们的DOM结构。

[00:21:31] 那接下来是计算样式，那CSS的样式主要来源有三种第一种是通过link标签应用web的CSS文件第二种是style标签内的CSS第三种是元素的style 属性内的 CSSCSS其实浏览器也不认识需要把它转化成CSSOM 树如果想要看看他们的话我们也可以在控制台输入document.styleSheets变量来查看，接下来我们就可以把样式进行一个标准化了CSS其实还有很多简化的写法比如说color: green会转化成color: rgb(0, 128, 0)1em会被转化成32px，最后计算出DOM树每个节点的具体样式这里涉及到CSS的一些特点就是CSS具有继承性每个子节点都包含着父节点的样式，接下来就是布局了因为我们现在有了DOM 结构和已经计算好的样式。

[00:22:25] 现在我们可以把它们进行一个合成操作这里我们通过遍历的方式选中所有可见的节点把节点添加到布局树中创建好布局树之后我们可以对布局进行计算也就是我们每个节点在页面的具体位置在绘制页面之前还需要经历一个分层的部分分层其实是为了实现层级的操作比如说CSS的z-index属性或者页面滚动和 3D 变换等属性等我们可以快速通过开发者工具的Layers工具来查看当前页面的分层情况这些图层最终会全部叠在一起最终形成一个平面的页面，接下来给节点分完层之后我们就可以开始对每个图层进行一个绘制操作了绘制就是把复杂的页面分成非常多步，然后逐一进行绘制我们可以打开开发者工具选择Layers 工具，然后选择document的层来看一下具体的绘制步骤绘制列表是用来记录绘制命令的一个顺序的。

[00:23:22] 但是绘制操作其实是有渲染进程中新建的合成线程来完成的渲染进程的主线程完成了构建DOM 到绘制列表的操作，然后将绘制列表交给合成线程在一个页面中合成线程会将图层划分成图块这些图块一般是256×256像素或者是512×512像素在整个页面中浏览器可以展示的部分叫做视口合成线程会把视口附近的图块优先转化成位图而转化成位图的操作其实就是栅格化因为一个页面的图块会有很多它们一般是并行进行处理的，所以渲染进程专门有一个栅格化的线程池栅格化一般会有GPU进行加速因为我们之前讲了GPU有单独的进程，所以这里涉及到跨进程操作图块在GPU中被转化成位图并保存在GPU的内存中一旦所有图块都被栅格化合成线程就会生成一个绘制图块的命令给浏览器进程浏览器进程收到后就会把页面的内容绘制到内存中。

[00:24:23] 最后将内存中的图像显示到屏幕中，那么到目前为止我们的一个大致的渲染流程就已经跟大家讲完了，那接下来跟大家继续分享一些有关这个渲染进程的一些其他的操作，那就是重排、重绘还有合成，那我觉得如果大家对前面的流程比较熟悉的话，那这三个东西其实还是非常好理解的，首先跟大家说一下这个重排重排其实就是当我们修改了元素的宽高或者新增删除的元素都会触发重新布局，那我们可以看到这条渲染进程的流水线上我们布局之后的东西都得再跑一遍，所以这个重排的开销是非常大的，那重绘因为我们没有改变相对位置啊而是改变了其他的样式属性，所以我们跳过了布局和分层这一步骤执行效率会比重排要高一些，那合成的话其实它本质上是利用了浏览器的一个分层机制来进行了一些处理和变化。

[00:25:16] 那么类似CSS 的 transform啊他跳过了布局和绘制的阶段，所以只进行栅格化的一些操作因为栅格化操作并不占用主线程，所以它的效率比重排和重绘要高很多，好了，那这就是本期视频的全部内容了如果大家觉得还不错的话可以三连支持一下，那我们下期再见886。
