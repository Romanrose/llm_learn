# 内容阅读、视频转写与课程笔记：使用手册

> 直接贴链接并说明想要的结果即可。本机优先走**已有字幕**；只有没有可用字幕时，才在你明确授权后下载音频并本地转写。

## 先选目标

| 你想做什么 | 直接这样说 |
| --- | --- |
| 读文章/网页 | `用已登录的 Chrome 读取并总结：<链接>` |
| 找 B 站视频 | `搜索 B 站关于“<主题>”的高质量视频，推荐 5 个并说明理由。` |
| 快速了解视频 | `总结这个视频；优先使用字幕。若无字幕，只输出结构化摘要，不下载音频。<链接>` |
| 完整逐字稿 | `把这个视频转成完整中文 Markdown 逐字稿，保留时间戳。<链接>` |
| 课程笔记 | `把这个课程视频整理为中文 Obsidian 笔记：完整逐字稿另存，另写讲义版。<链接>` |
| 只要字幕文件 | `提取该视频的字幕，输出 SRT、VTT 和清理后的 Markdown。<链接>` |

## 视频转写：默认决策树

```text
视频 URL / 本地音频
  ├─ 有官方字幕或自动字幕 → 下载、清洗、去重 → Markdown
  └─ 无可用字幕
       ├─ 未授权下载音频 → 先询问
       └─ 已授权 → yt-dlp 提取音频 → 本地 ASR → Markdown / SRT / VTT
```

### 生成完整逐字稿

```text
把这个视频转成完整中文 Markdown 逐字稿，保留段落级时间戳。
优先提取原字幕；若没有字幕，先征求我的授权后下载音频，
使用本机 Qwen3-ASR 转写。只修正明显断句、重复和术语错词，
不要压缩成摘要。将连续语义合并为自然段，不要每句或每个时间片单独换行；
另存为 <目标路径>。

<链接>
```

### 生成课程讲义

```text
把这个课程视频转成中文 Obsidian 笔记。
先产出完整逐字稿，再另写一份讲义版：按概念、公式、结论和待实践项组织；
公式使用 $行内公式$ 与 $$块级公式$$。保留原视频链接。

<链接>
```

### 文件约定

- `lectureN_transcript.md`：完整逐字稿，使用段落级时间戳。
- `lectureN.md`：讲义版，允许重组内容、补充公式与链接。
- 原始音频、字幕下载件、JSON 等中间文件默认放临时目录；只有你明确要求才存入 Obsidian。

### 逐字稿排版规则

- 正文默认按**完整语义段**排版：每段约 3–8 句、300–800 个中文字符；讲者换人、话题转折或停顿较长时再换段。
- 时间戳只放在段首，例如：`[12:34] 接下来讨论线性注意力……`；不把每一个 ASR 片段都写成一行。
- 删除自动字幕造成的相邻重复、半句重启与无意义口头填充；不删除影响论证的内容。
- 需要精确回听、逐词定位时，额外输出 `.srt` / `.vtt`；Markdown 正文仍以阅读体验优先。
- 中文译稿保留术语首现的英文，如“混合专家（Mixture of Experts, MoE）”；后续可直接使用缩写。

## 本机能力与模型选择

本机项目：

- 下载器：[workflow/video/yt-dlp](/Users/romanrose/Obsidian/llm_learn/workflow/video/yt-dlp)
- 转写环境：[workflow/video/transcript-pipeline](/Users/romanrose/Obsidian/llm_learn/workflow/video/transcript-pipeline)
- 媒体处理：`ffmpeg`

| 场景 | 首选 | 原因 |
| --- | --- | --- |
| CS336 等中英技术课 | `Qwen3-ASR-0.6B` | 已在 Apple M5/MPS 成功加载；更适合技术术语与中英混合。 |
| 英文长视频或资源紧张 | `faster-whisper` | 更轻、更稳定，是可靠回退。 |
| 中文访谈/课程 | Qwen3-ASR；必要时再与 SenseVoice 对照 | 不为普通任务重复跑多模型。 |
| 多人会议、逐词定位 | 后续启用 WhisperX | 提供词级时间戳与说话人标签。 |

## 原有字幕工作流 vs. 本机转写环境

| 能力 | 原有 `video-subtitle-transcript` 工作流 | 本机 `transcript-pipeline` | 结论 |
| --- | --- | --- | --- |
| 视频获取 | 用 `yt-dlp` 列出并下载字幕 | 用 `yt-dlp` 下载字幕或音频 | 重合；只保留一个下载入口即可。 |
| 字幕优先 | 官方字幕优先，其次自动字幕 | 应复用同一策略 | 完全复用。 |
| 字幕清洗 | 解析 VTT/SRT/ASS/JSON；去时间码、标签、重复片段 | 尚未实现 | 复用原有脚本，不重写。 |
| Markdown 元数据 | 写入来源、字幕文件、生成时间、语言 | 应复用同一格式 | 完全复用。 |
| 无字幕后的 ASR | 明确要求先征求授权，再交给其他 STT 工具 | Qwen3-ASR / faster-whisper 本地转写 | 本机环境补齐的核心能力。 |
| SRT/VTT、词级时间戳、说话人 | 不负责 | 计划支持；WhisperX 用于高精度需求 | 本机环境的扩展能力。 |

**统一原则：不做两套字幕解析器。**原有工作流继续负责“字幕 → 干净文稿”；本机项目只增加“无字幕时，音频 → ASR 结果”，然后输出接入同一份清洗与 Markdown 格式。

## 登录态、授权与边界

- 公开视频：先尝试公开字幕。
- 无字幕：我会明确询问是否下载音频并转写。
- 需要登录态：每次明确授权后才使用对应浏览器 cookies；不把 cookies 写入项目配置、命令历史或笔记。
- 不绕过私密内容、付费墙、地区限制或验证码。
- 点赞、评论、发帖、订阅等外部操作始终单独确认。

## 当前开发状态

`transcript-pipeline` 的 Python 3.12 环境、`ffmpeg`、`faster-whisper` 和 Qwen3-ASR 已配置完成；URL → Markdown/SRT/VTT 的命令封装尚待实现。在它完成前，直接使用上方自然语言提示；处理时仍会按这份决策树执行。
